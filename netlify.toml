[build]
  command = "prisma generate && next build"
  publish = ".next"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 移除錯誤的 edge_functions 設定，改用標準模式
[[plugins]]
  package = "@netlify/plugin-nextjs"
